有n种重量和价值分别为w_i,v_i的物品。从这些物品中挑选总重量不超过W（大写）的物品，求挑出物品价值总和的最大值。
在这里，每种物品可以挑选任意多件。
1 <= n <= 100

输入 n = 3
     (w,v) = {(3,4),  (4,5)  ,(2,3)}
     W（大写） = 7

输出
   10


#include <cstdlib>
#include<iostream>
#include<algorithm>
#include<vector>

using namespace std;

int max(int b,int c);
vector<int>w12;
vector<int>v12;

int main()
{
    int n,a;
    
    cout <<"n=";
    cin >> n;
    cout << endl;
    int i = 0;
    while(i < n)
    {
    int w[i];
    int v[i];
     
       cout << "("; 
       cin >> w[i];
       cout<< ",";
       cin >> v[i];
       cout  <<") ,";
       w12.push_back(w[i]);
       v12.push_back(v[i]);
       i++;
    }
    
   cout << endl<<"a = " ;
   cin >> a;
   cout << endl;
   int dp[n][a];
    for (int i = 0;i < n;i++)
    {
        for(int j = 0;j <= a;j++)
        {
            for (int k = 0;k *w12[i] <= j;k++)
            {
                
             dp[i+1][j] = max(dp[i+1][j],dp[i][j-k*w12[i]] +k*v12[i]);
                
            }
        }
    }
 cout << dp[n][a];    
 char response;
    cin >> response;
    return 0;   
}

int max(int b,int c)
{
    if (b > c)
    {
        return b;
    }
    else
    {
        return c;
    }     
}
